# Frontend (Vite) — safe to expose in the browser bundle
VITE_SERVER_NAME="Наш сервер Arma 3"
VITE_ORDER_API_URL="/api/order"

# Backend (api/order.js serverless function) — NEVER commit real values
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=
